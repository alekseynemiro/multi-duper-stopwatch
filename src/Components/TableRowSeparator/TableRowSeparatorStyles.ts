import { StyleSheet } from "react-native";
import { styles } from "@styles";

export const tableRowSeparatorStyles = StyleSheet.create({
  separator: {
    ...styles.border,
    marginVertical: 8,
  },
});
